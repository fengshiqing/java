<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mappers.StudentMapper"><!-- 对应接口的限定名 -->

	<insert id="add" parameterType="com.mybatis.model.Student" ><!-- parameterType="Student" 因为mybatis-config里配置了别名，所以这里可以这么用 -->
		insert into t_student (id, name, age) values( null, #{name}, #{age} )
	</insert>
	
	<update id="update" parameterType="com.mybatis.model.Student">
		update t_student set name=#{name}, age=#{age} where id=#{id}
	</update>
	
	<delete id="delete" parameterType="com.mybatis.model.Student">
		delete from t_student where id=#{id}
	</delete>
	
	<select id="findById111" parameterType="Integer" resultType="com.mybatis.model.Student">
		select * from t_student where id=#{id}
	</select>
	
	<resultMap type="com.mybatis.model.Student" id="StudentResult">
		<id property="id" column="id"/><!-- id标签是用来定义主键的 -->
		<result property="name" column="name"/><!-- result标签是定义普通列的 -->
		<result property="age" column="age"/>
		
		<result property="address.id" column="addressId"/>
		<result property="address.sheng" column="sheng"/>
		<result property="address.shi" column="shi"/>
		<result property="address.qu" column="qu"/>
	</resultMap>
	
	<select id="find" resultMap="StudentResult">
		select * from t_student
	</select>
	
	<select id="findStudentWithAddress" resultMap="StudentResult">
		select * from t_student t1, t_address t2
		 where t1.addressId = t2.id
		   and t1.id=#{id}
	</select>
	
	<insert id="insertStudents" parameterType="com.mybatis.model.Student" ><!-- parameterType="Student" 因为mybatis-config里配置了别名，所以这里可以这么用 -->
		insert into t_student values( null, #{name}, #{age}, #{pic}, #{remark} )
	</insert>
	
	<select id="queryStudentById" parameterType="int" resultType="com.mybatis.model.Student">
		SELECT	* 
		FROM	t_student
		WHERE	id = #{id}
	</select>
	
	<!-- 分页查询——逻辑分页 -->
	<select id="queryStudentRowBound" resultType="com.mybatis.model.Student">
		SELECT	* 
		FROM	t_student
	</select>
	
	<!-- 分页查询——物理分页 -->
	<select id="queryStudentLimit" parameterType="Map" resultType="com.mybatis.model.Student">
		SELECT	* 
		FROM	t_student
		<if test="start !=null and size != nul">
			limit #{start}, #{size}
		</if>
	</select>
	
</mapper>
