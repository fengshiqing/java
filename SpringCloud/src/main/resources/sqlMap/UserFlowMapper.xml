<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kunning.springcloud.mapper.UserFlowMapper">

    <resultMap id="BaseResultMap" type="com.kunning.springcloud.service.Product">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="code" jdbcType="VARCHAR" property="code"/>
        <result column="price" jdbcType="INTEGER" property="price"/>
    </resultMap>

    <select id="selectByPrimaryKey" resultType="com.kunning.springcloud.service.Product">
        SELECT id, `name`, code, price
        FROM t_user_flow
        WHERE id = #{id}
    </select>

    <select id="selectProductByPage" resultType="com.kunning.springcloud.service.Product">
        SELECT id, `name`, code, price
        FROM t_user_flow
        # 从 第offset条记录开始(不包含offset)，返回pageSize条记录。例如：LIMIT 5,10; // 检索记录行 6-15
        # 换句话说，LIMIT n 等价于 LIMIT 0,n。
        LIMIT #{offset}, #{pageSize}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE
        FROM t_user_flow
        WHERE id = #{id}
    </delete>

    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.kunning.springcloud.service.Product"
            useGeneratedKeys="true">
        insert into t_user_flow (`name`, code, price)
        values (#{name,jdbcType=VARCHAR}, #{code,jdbcType=VARCHAR}, #{price,jdbcType=INTEGER})
    </insert>

    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.kunning.springcloud.service.Product"
            useGeneratedKeys="true">
        insert into t_user_flow
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">
                `name`,
            </if>
            <if test="code != null">
                code,
            </if>
            <if test="price != null">
                price,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="code != null">
                #{code,jdbcType=VARCHAR},
            </if>
            <if test="price != null">
                #{price,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.kunning.springcloud.service.Product">
        update t_user_flow
        <set>
            <if test="name != null">
                `name` = #{name,jdbcType=VARCHAR},
            </if>
            <if test="code != null">
                code = #{code,jdbcType=VARCHAR},
            </if>
            <if test="price != null">
                price = #{price,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.kunning.springcloud.service.Product">
        update t_user_flow
        set `name` = #{name,jdbcType=VARCHAR},
            code   = #{code,jdbcType=VARCHAR},
            price  = #{price,jdbcType=INTEGER}
        where id = #{id}
    </update>

</mapper>