<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <!-- %d日期时间，%p日志级别，%t线程名，%c类的全名，%m输出的信息，%i索引【从数字0开始递增】,,, -->
    <!-- appender 是 configuration 的子节点，是负责写日志的组件。 -->
    <!-- ConsoleAppender：把日志输出到控制台 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder><!-- pattern节点，用来设置日志的格式 -->
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%5p] --- [%15t] [%file:%line] --- %m%n</pattern>
            <charset>UTF-8</charset><!-- 控制台也要使用UTF-8，不要使用GBK，否则会中文乱码 -->
        </encoder>
    </appender>
    <!-- RollingFileAppender：滚动记录文件，先将日志记录到指定文件，当符合某个条件时，将日志记录到其他文件 -->
    <!-- 以下的大概意思是：1.先按日期存日志，日期变了，将前一天的日志文件名重命名为XXX%日期%索引，新的日志仍然是sys.log -->
    <!--             2.如果日期没有发生变化，但是当前日志的文件大小超过1KB时，对当前日志进行分割 重命名-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %5p --- [%15t] [%file:%line] --- %m%n</pattern>
            <charset>UTF-8</charset><!-- 设置日志的编码 -->
        </encoder>
        <File>C:/workspace/logs/springboot.log</File><!-- D盘，默认是代码所在盘的根目录 -->
        <!-- rollingPolicy:当发生滚动时，决定 RollingFileAppender 的行为，涉及文件移动和重命名。 -->LoggerContext
        <!-- TimeBasedRollingPolicy： 最常用的滚动策略，它根据时间来制定滚动策略，既负责滚动也负责出发滚动 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 活动文件的名字会根据fileNamePattern的值，每隔一段时间改变一次 -->
            <!-- 文件名：springboot.2019122523.0.log -->
            <fileNamePattern>/workspace/logs/springboot.%d{yyyyMMddHH}.%i.log</fileNamePattern>
            <!-- 每产生一个日志文件，该日志文件的保存期限为30天 -->
            <maxHistory>360</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10240KB</maxFileSize><!-- maxFileSize:这是活动文件的大小，默认值是10MB,本篇设置为1KB，只是为了演示 -->
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>

    <!-- 控制台输出日志级别 -->
    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>
    <!-- 指定项目中某个包的代码的日志级别，以"com.kunning.springboot"为例，此包及其子包下面的所有日志记录级别都是DEBUG -->
    <!-- 级别【从高到低】依次为：FATAL > ERROR > WARN > INFO > DEBUG > TRACE  -->
    <logger name="com.kunning.springboot" level="DEBUG">
        <appender-ref ref="file"/>
    </logger>
</configuration>